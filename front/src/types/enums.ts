/**
 * All application enums centralized in one file
 * Convention: Enum keys are in UPPERCASE with underscores, values are in lowercase
 */

// User role enum
export enum EUserRole {
  ADMIN = 'admin',
  USER = 'user',
}
